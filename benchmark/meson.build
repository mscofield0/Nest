# =========== Dependencies
benchmark_deps = []
benchmark_dep_libs = []

subdir('deps')

# =========== Source files
benchmark_bootstrap = files('Bootstrap.cxx')
benchmark_src = [benchmark_bootstrap]

subdir('src')