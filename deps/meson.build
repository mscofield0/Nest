spdlog_dep = dependency('spdlog', 
  version: '1.9.2',
  fallback: ['spdlog', 'spdlog_dep']
)

fmt_dep = dependency('fmt',
  version: '8.0.1',
  fallback: ['fmt', 'fmt_dep']
)

lib_deps += [
  spdlog_dep,
  fmt_dep,
]